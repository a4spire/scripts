# Wird beim Next.js-Build inline in den Client-Bundle übernommen (nicht zur Laufzeit änderbar!).
# Bei Docker Compose entsprechend als Build-Arg (siehe docker-compose.yml) übergeben.
NEXT_PUBLIC_API_URL=http://localhost:4000
